# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=win-spares-secret-key-2026

# Firebase Credentials Configuration
# Path to service account JSON file (relative to app root or absolute)
FIREBASE_CREDENTIALS_PATH=serviceAccountKey.json

# Storage Directory for Local PDF / Cache (if applicable)
PDF_STORAGE_DIR=generated
